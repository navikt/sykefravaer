const hentArbeidsrettetOppfolgingBilde = (bildefil) => {
    return `${process.env.REACT_APP_CONTEXT_ROOT}/img/svg/arbeidsrettet-oppfolging/${bildefil}`;
};

export default hentArbeidsrettetOppfolgingBilde;

